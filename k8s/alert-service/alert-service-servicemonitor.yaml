apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: alert-service
  namespace: default
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: alert-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s